postgresql:
  image:
    tag: "14"
    pullPolicy: IfNotPresent
  global:
    postgresql:
      service: 
        ports:
          postgresql: 5432
  auth:
    postgresPassword: eyerys
    username: user
    password: eyerys
    database: bdd
 
# extendedConfConfigMap:
  #  initdbScripts:
   #   my_init_script.sql: |
security:
  networkPolicy: 
    enabled: true

